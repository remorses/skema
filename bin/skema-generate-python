#!/usr/bin/env python
import sys
from skema.generate import generate_types


if len(sys.argv) != 2:
    print('needs exactly 1 argument, path')

generate_types(
    sys.argv[-1], 
    '.py', 
    '--python-version 3.7 --just-types ' #--no-nice-property-names'
)
